version: "3.9"

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: automation
      POSTGRES_PASSWORD: automation
      POSTGRES_DB: automation_db
    ports:
      - "5432:5432"
